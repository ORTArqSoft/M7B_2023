//telnet localhost 8080
const net = require("node:net");

const server = net.createServer((socket) => {
  socket.write("Hello, Node.js TCP Server!\n");
  socket.end();
});

server.listen(8080, () => {
  console.log("TCP server listening on port 8080");
});
